{
    "name": "hour-ledger/ecosystem",
    "description": "Hour Ledger Ecosystem monorepo root",
    "type": "project",
    "private": true,
    "repositories": [
        {
            "type": "path",
            "url": "packages/backend/*",
            "options": {
                "symlink": true
            }
        }
    ],
    "scripts": {
        "test": [
            "@php -r \"echo 'Run app/package specific tests.\\n';\""
        ]
    }
}
